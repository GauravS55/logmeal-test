# LogMeal Management Microservice


# How to Run
- To build the module and subsections using docker-compose
`$ docker-compose build`

- To run the project
`$ docker-compose up`

# How to open Frontend PORT

- localhost:3000